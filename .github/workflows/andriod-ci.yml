name: Teacher Android Automation CI

on:
  pull_request:
    branches: ["dev"]     # Smoke Tests

  push:
    branches: ["main"]    # Regression Tests

  schedule:
    - cron: "0 2 * * *"   # Nightly Regression

  workflow_dispatch:

jobs:

  # =========================================================
  # SMOKE TESTS
  # =========================================================
  smoke-tests:
    name: Smoke Tests
    if: github.event_name == 'pull_request'
    runs-on: [self-hosted, Windows]

    env:
      APPIUM_PORT: 4723
      APP_PACKAGE: com.OritSciencesPrivateLimited.EnglishGurukul.teacher
      APP_ACTIVITY: com.OritSciencesPrivateLimited.EnglishGurukul.MainActivity

    steps:

      # ================= CHECKOUT =================
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ================= PYTHON =================
      - name: Verify Python
        run: |
          python --version
          pip --version

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      # ================= NODE =================
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Verify Node
        run: |
          node -v
          npm -v

      # ================= APPIUM =================
      - name: Install Appium
        run: |
          npm install -g appium
          appium -v

      - name: Install UiAutomator2 Driver
        run: |
          appium driver install uiautomator2

      # ================= JAVA =================
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Verify Java
        run: |
          java -version

      # ================= ANDROID DEVICE =================
      - name: Verify Android Device
        run: |
          adb devices

      # ================= KILL OLD APPIUM =================
      - name: Kill Existing Appium Server
        shell: powershell
        run: |
          $port = 4723

          $connections = Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue

          if ($connections) {
            $pids = $connections | Select-Object -ExpandProperty OwningProcess -Unique

            foreach ($pid in $pids) {
              Stop-Process -Id $pid -Force -ErrorAction SilentlyContinue
              Write-Host "Killed process ID $pid using port $port"
            }
          }
          else {
            Write-Host "No Appium process running on port $port"
          }

      # ================= START APPIUM =================
      - name: Start Appium Server
        shell: powershell
        run: |
          Start-Process cmd -ArgumentList "/c appium --address 127.0.0.1 --port 4723"
          Start-Sleep -Seconds 15

      # ================= VERIFY APPIUM =================
      - name: Verify Appium Server
        shell: powershell
        run: |
          Invoke-WebRequest -Uri http://127.0.0.1:4723/status

      # ================= RUN TESTS =================
      - name: Run Smoke Tests
        run: |
          pytest -m smoke -v -s --alluredir=allure-results

      # ================= ALLURE =================
      - name: Install Allure CLI
        run: |
          npm install -g allure-commandline
          allure --version

      - name: Generate Allure Report
        run: |
          allure generate allure-results --clean -o allure-report

      # ================= REPORTS =================
      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: smoke-allure-report
          path: allure-report

      - name: Upload Failure Screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-failure-screenshots
          path: screenshots/


  # =========================================================
  # REGRESSION TESTS
  # =========================================================
  regression-tests:
    name: Regression Tests
    if: github.event_name != 'pull_request'
    runs-on: [self-hosted, Windows]

    env:
      APPIUM_PORT: 4723
      APP_PACKAGE: com.OritSciencesPrivateLimited.EnglishGurukul.teacher
      APP_ACTIVITY: com.OritSciencesPrivateLimited.EnglishGurukul.MainActivity

    steps:

      # ================= CHECKOUT =================
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ================= PYTHON =================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Verify Python
        run: |
          python --version
          pip --version

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ================= NODE =================
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Verify Node
        run: |
          node -v
          npm -v

      # ================= APPIUM =================
      - name: Install Appium
        run: |
          npm install -g appium
          appium -v

      - name: Install UiAutomator2 Driver
        run: |
          appium driver install uiautomator2

      # ================= JAVA =================
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Verify Java
        run: |
          java -version

      # ================= ANDROID DEVICE =================
      - name: Verify Android Device
        run: |
          adb devices

      # ================= KILL OLD APPIUM =================
      - name: Kill Existing Appium Server
        shell: powershell
        run: |
          $port = 4723

          $connections = Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue

          if ($connections) {
            $pids = $connections | Select-Object -ExpandProperty OwningProcess -Unique

            foreach ($pid in $pids) {
              Stop-Process -Id $pid -Force -ErrorAction SilentlyContinue
              Write-Host "Killed process ID $pid using port $port"
            }
          }
          else {
            Write-Host "No Appium process running on port $port"
          }

      # ================= START APPIUM =================
      - name: Start Appium Server
        shell: powershell
        run: |
          Start-Process cmd -ArgumentList "/c appium --address 127.0.0.1 --port 4723"
          Start-Sleep -Seconds 15

      # ================= VERIFY APPIUM =================
      - name: Verify Appium Server
        shell: powershell
        run: |
          Invoke-WebRequest -Uri http://127.0.0.1:4723/status

      # ================= RUN TESTS =================
      - name: Run Regression Tests
        run: |
          pytest -m regression -v -s --alluredir=allure-results

      # ================= ALLURE =================
      - name: Install Allure CLI
        run: |
          npm install -g allure-commandline
          allure --version

      - name: Generate Allure Report
        run: |
          allure generate allure-results --clean -o allure-report

      # ================= REPORTS =================
      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: regression-allure-report
          path: allure-report

      - name: Upload Failure Screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: regression-failure-screenshots
          path: screenshots/
